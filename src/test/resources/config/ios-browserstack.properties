# iOS on BrowserStack real device (uses the RealDevice .ipa)
device.name=iPhone 15
platform.version=17
# bs:// app id returned by the upload API (set via BROWSERSTACK_APP_IOS in CI)
app.path=${BROWSERSTACK_APP_IOS}
