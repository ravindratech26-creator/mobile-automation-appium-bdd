# Android on BrowserStack real device
device.name=Google Pixel 8
platform.version=14.0
# bs:// app id returned by the upload API (set via BROWSERSTACK_APP_ANDROID in CI)
app.path=${BROWSERSTACK_APP_ANDROID}
